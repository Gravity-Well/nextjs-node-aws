import Layout from '../components/Layout';

const Login = () => {
console.log('hello')// show up in browser console and Terminal window when refreshed
return(
<Layout>login page</Layout>
)
}

export default Login;
